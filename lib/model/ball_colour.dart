/// The base colour for a ball.
/// These values are just keys which can be mapped to whichever colour scheme is
/// required.
enum BallColour {
  colour1,
  colour2,
  colour3,
  colour4,
  colour5,
  colour6,
  colour7,
}
